{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "observer-mcp.serviceAccountName" . }}
  labels:
    {{- include "observer-mcp.labels" . | nindent 4 }}
automountServiceAccountToken: false
{{- end }}
